import {
    BrowserRouter as Router,
    Switch,
    Route,
    Link
  } from "react-router-dom";

  const routes = (
    <Switch>
      <Route path="/users/dashboard" components="" />
    </Switch>
  );

  export default routes;